# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2021 Intel Corporation

---
all:
  vars:
    cluster_name: cluster    # NOTE: Use `_` instead of spaces.
    flavor: minimal  # NOTE: Flavors can be found in `flavors` directory.
    single_node_deployment: false  # Request single node deployment (true/false).
    limit:                        # Limit ansible deployment to certain inventory group or hosts
controller_group:
  hosts:
    controller:
      ansible_host: 10.0.13.181
      ansible_user: openness
edgenode_group:
  hosts:
    node01:
      ansible_host: 10.0.13.72
      ansible_user: openness
edgenode_vca_group:
  hosts:
ptp_master:
  hosts:
ptp_slave_group:
  hosts:
